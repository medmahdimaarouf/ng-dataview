import { ItemDirectiveDirective } from './item-directive.directive';

describe('ItemDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
